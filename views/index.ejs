<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/styles.css" />
		<title>Message Board</title>
	</head>
	<body>
		<h1>HOME PAGE</h1>
		<ul>
			<% messages.map(message => { %>
			<li>
				<span id="left"><%=message.text%> - <%=message.user%></span>
				<span id="right"
					><a class="open" href="<%= `/new/${message.id}` %>">Open</a></span
				>
			</li>
			<%})%>
		</ul>
		<a href="/new" role="button" tabindex="0">Add new message</a>
	</body>
</html>
