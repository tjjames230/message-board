<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/styles.css" />
		<title>Message Board</title>
	</head>
	<body>
		<h1>MESSAGE DETAILS</h1>
		<ul>
			<% messages.forEach(message => { if (id == message.id) { %>
			<div>
				<h3><%=message.user%></h3>
				<p><%=message.text%></p>
				<p><%=message.added%></p>
			</div>
			<% }}) %>
		</ul>
	</body>
</html>
